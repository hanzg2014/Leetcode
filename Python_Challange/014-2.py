#coding: utf8
#!/usr/bin/python

from PIL import Image

image = Image.new("RGB",(100,100))
wire = Image.open("wire.png")
pxlist = list(wire.getdata())

def move(pos, direction ,step)
    
    return 



for n in range (0,10000,1):
    image.putpixel((x,y), pxlist(n))

#    2*i, 
#    2*i-2, 2*i-1, 2*i-2
#imdata = list(wire.getdata())
#print imdata

#def move(position, direction, step):
#    if (dicretion == 0)

